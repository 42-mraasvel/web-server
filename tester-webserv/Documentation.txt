-- Main Concept --

	1. Give the Host:Port as argument to the Tester

	- Request TestCase class
	- Write testcases in the code itself by form of a class, similar to catch

-- API used --

	https://docs.python-requests.org/en/latest/api/

-- Dependencies --

	- Requests module
		`python -m pip install requests`