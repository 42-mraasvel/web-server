********** python tester **********

-- General --
	?- Config File Error
	?- Connection Management
	v- Method Execution (response codes, body, etc)
		- GET
			- 200
			- 404 NOT_FOUND
			- 403 FORBIDDEN (file)
		- POST
			- with upload_store (201 CREATED, 204 NO_CONTENT)
			- w/o upload_store >> root (201 CREATED, 204 NO_CONTENT)
			- 403 FORBIDDEN (file, directory)
		- DELETE
			- 204 NO_CONTENT
			- 404 NOT_FOUND
			- 403 FORBIDDEN (file, directory)
	?- Response Codes
		x- 100 CONTINUE (see c++ tester)
		v- 201 CREATED (see Method Execution - POST)
			- Location header should be request target
		v- 204 NO_CONTENT (see Method Execution - POST/DELETE)
		v- 301 MOVED_PERMANENTLY (see ServerBlock-ErrorPage)
		x- 3xx REDIRECT  (see c++ tester)
		x- 400 BAD_REQUEST (see c++ tester)
		v- 403 FORBIDDEN (see Method Execution)
		v- 404 NOT_FOUND (see Method Execution)
		v- 405 METHOD_NOT_ALLOWED (see LocationBlock-MethodAllowed)
		?- 408 REQUEST_TIMEOUT
		v- 413 PAYLOAD_TOO_LARGE (see ServerBlock-ClientMaxSize)
		?- 415 UNSUPPORTED_MEDIA_TYPE
		v- 417 EXPECTATION_FAILED (see StatusCode)
		?- 431 REQUEST_HEADER_FIELDS_TOO_LARGE
		v- 500 INTERNAL_SERVER_ERROR (through out whole program)
		v- 501 NOT_IMPLEMENTED (see StatusCode)
		?- 502 BAD_GATEWAY
		?- 504 GATEWAY_TIMEOUT
		x- 505 HTTP_VERSION_NOT_SUPPORTED (see c++ tester)
		
	?- Response Header-Fields
		- Retry
		- Connection
		v- Allow (see Location Block - Method allowed)
		- Content-Type
	?- Test with different settings.hpp files (BUFFER_SIZE, LIMITS)
		- Chunked

-- Server Block directive --

	v- Listen: Interface (host:port) resolution
		- same server_name but different Interface
	v- Server_name: resolution
		x- empty server_name / empty host header (see c++ tester)
		- default block: take first server if no server_name match
		- wildcard (exactmatch > wildcard, longest, front, back, front > back, )
	v- Default error page
		- Force a 404 error: Status Code and body should match the error page
		- Error with nonexistant error page
	v- Client Max Body Size
		- max body too small

-- Location Block directive--

	v- Location block resolution
		- match | not found | exact match
	v- Method allowed (GET, POST, DELETE)
		- HeaderField (allowed: x)
	v- Root Directory (empty root, root syntax error)
	v- Auto Index
	v- Default File
	?- CGI: executable to test in/output
		- Header-Fields
		- StatusCode
		- Environment variables
	v- upload_store (see Method Execution - POST)
	v- return/redirection (GET/POST/DELETE)

********** c++ client program **********
	- 400 Bad Request from RequestParser
		- target error
		- case sensitivity
	- 505 HTTP_VERSION_NOT_SUPPORTED
	- 100 CONTINUE
	- 3xx REDIRECT (direct response)
		- Location header-field
		- Response Code (for different redirection types)
		- Retry-After header-field
	- HTTP version 1.0
	- Duplicate header-fields
	- Empty host field to empty server_name resolution
	- Interface resolution (ServerBlock resolution based on IP:PORT specification, all requests go in through a single interface)
	- Timeout
	- Syntax Checks
	- RequestValidator functions: check one by one




********** question **********
1.
for below the case, a request from 127.0.0.1:8081 with Host: second_server
will resolve to first block instead of the second.
server {
	listen 8081;
	listen 127.0.0.1:8081;
	server_name first_server;

	location / {
		allowed_methods GET;
		root ./ServerRoot/FirstServer/8081;
		index index.html;
	}
}

server {
	listen 8081;
	server_name second_server;

	location / {
	}
}
